{
  "buildCommand": "pnpm build:all && pnpm build:app",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "build": {
    "env": {
      "NODE_VERSION": "20.9.0"
    }
  }
}
